# NestHelper Service Card Alignment Fix

Replacement files only.

This update fixes the specialty service cards so the rows stretch evenly, card content has breathing room, buttons are no longer absolutely pinned/cropped, and the specialty section waits until extra-wide screens before using four narrow columns.

Files included:
- components/ServiceCard.tsx
- app/services/page.tsx
