# Move Prep form simplification update

This patch simplifies the Move Prep & Home Reset request form UI.

Changes:
- Renames the package dropdown to “Choose the main starting package.”
- Shortens package labels so the dropdown is easier to scan.
- Changes duplicate checkboxes into “What else should we include or quote?” add-ons.
- Makes it clearer that move-out cleaning is quoted separately.
- Adds short pricing cards instead of one long pricing paragraph.
- Keeps the moving-company disclaimer and existing request/admin flow.

Files changed:
- components/forms/RequestForm.tsx
- README_MOVE_PREP_FORM_SIMPLIFY_UPDATE.md
