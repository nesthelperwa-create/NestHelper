# Sitemap Last Modified Update

Small sitemap-only update.

## Changed

- Updated `app/sitemap.ts` sitemap `lastModified` value from `2026-06-17` to `2026-06-25`.
- Kept the existing sitemap route list and priorities unchanged.
- Did not add admin, checkout, API, or query-string request URLs.

## Notes

Direct request links such as `/request?service=specific-area-reset#request-form` and `/request?service=move-in-out-cleaning#request-form` should not be separate sitemap entries. The canonical `/request` page remains in the sitemap.

No dependencies changed.
