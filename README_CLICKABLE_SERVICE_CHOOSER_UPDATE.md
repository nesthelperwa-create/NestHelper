# Clickable Service Chooser Update

Small Services page update only.

## Changed

- Removed the left-side Request Help button in the Services page chooser section.
- Added helper text telling customers to click the matching service guide on the right.
- Made each “Not sure what to choose?” service guide card clickable.
- Each guide card jumps down to the matching service card below.
- Added scroll margin to service card anchors so the fixed header does not cover the target.

## Files changed

- `app/services/page.tsx`

## Notes

No dependency changes. No `npm install` needed unless `next` is missing locally.
