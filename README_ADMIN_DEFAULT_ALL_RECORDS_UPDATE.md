Admin default all records update

What this update does
- Makes the admin request list default to All records.
- Moves the All records work-queue button to the first position.
- Changes the quick reset button to Show all records when another queue bucket is active.
- Updates the active-filter count and clear-filters behavior so All records is treated as the default.

No Firebase rules deploy is needed.
