import { PageHero } from "@/components/PageHero";

export default function CheckoutPage(){return <><PageHero eyebrow="Checkout Flow" title="Payment happens after approval." text="NestHelper uses a request-first flow so families only pay after service area, scope, safety, staffing, promo code, and pricing have been reviewed."/><section className="mx-auto max-w-5xl px-4 py-14 sm:px-6 lg:px-8"><div className="grid gap-6 md:grid-cols-2"><Flow title="Regular services" steps={["Customer submits request","NestHelper reviews service area, scope, safety, pets, access, and availability","NestHelper sends secure Stripe checkout link by text/email","Stripe calculates applicable WA sales tax","Payment confirms the visit","NestHelper sends prep notes and final confirmation"]}/><Flow title="Laundry Rescue" steps={["Customer submits laundry request","NestHelper approves pickup and sends deposit link","Customer pays minimum deposit + applicable tax","Laundry is dry-weighed at pickup","Add-ons are confirmed","Final balance is sent by Stripe invoice/payment link before completion or delivery"]}/></div><div className="mt-8 rounded-[2rem] border border-nest-gold/20 bg-nest-gold/10 p-6"><h2 className="text-2xl font-black text-nest-teal">Important setup note</h2><p className="mt-2 text-nest-ink/75">In Stripe, set prices to tax-exclusive and enable automatic tax. Payment totals should show subtotal, discount if any, sales tax, and total due before the customer pays.</p></div></section></>}
function Flow({title,steps}:{title:string;steps:string[]}){return <div className="rounded-[2rem] bg-white p-6 shadow-soft"><h2 className="text-2xl font-black text-nest-teal">{title}</h2><ol className="mt-5 grid gap-3">{steps.map((s,i)=><li key={s} className="flex gap-3 rounded-2xl bg-nest-cream p-4"><span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-nest-teal text-sm font-black text-white">{i+1}</span><span className="font-semibold text-nest-ink/76">{s}</span></li>)}</ol></div>}
